{
  "name": "tools.nixfred.com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "./deploy.sh",
    "test": "bun astro build && ./tests/check-links.sh && ./tests/check-safety.sh && ./tests/check-copy.sh && bun tests/check-registry.mjs && bun tests/check-contrast.mjs",
    "test:links": "./tests/check-links.sh",
    "test:safety": "./tests/check-safety.sh",
    "test:copy": "./tests/check-copy.sh",
    "test:registry": "bun tests/check-registry.mjs",
    "test:contrast": "bun tests/check-contrast.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "astro": "^5.0.0"
  }
}
